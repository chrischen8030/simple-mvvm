part of '../../visitor_page.dart';

class _ActiveVotePageDesktop extends StatelessWidget {
  final VisitorPageModel model;
  _ActiveVotePageDesktop({required this.model});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        )
    );
  }
}
